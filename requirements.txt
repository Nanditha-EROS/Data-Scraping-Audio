# ==========================================================================
# Tamil Nadu Folk Song Dataset Pipeline - dependencies (local-only, no hosted APIs)
# Python 3.10 - 3.12 recommended.
# NOTE: ffmpeg, ffprobe and fpcalc (Chromaprint) are native binaries and must
#       also be installed on the system PATH (see README).
# ==========================================================================

# --- Config / orchestration / reliability ---
PyYAML>=6.0
tenacity>=8.2.0        # wait_exponential_jitter requires >= 8.2
tqdm>=4.66.0

# --- YouTube search + download ---
yt-dlp>=2024.8.6

# --- Metadata relevance gate ---
rapidfuzz>=3.6.0

# --- Audio I/O + DSP (integrity + quality gates) ---
soundfile>=0.12.1
librosa>=0.10.1
numpy>=1.24,<2.2
scipy>=1.11.0
pyloudnorm>=0.1.1

# --- Torch stack (VAD, torchaudio decode, classifier). CPU-only build. ---
# CPU wheels are fine here (no CUDA). If you ever move to GPU, install the
# matching CUDA wheel from https://pytorch.org first, then the rest of this file.
torch>=2.1.0
torchaudio>=2.1.0

# --- Music/Speech/Mixed classifier (chosen: PANNs Cnn14, CPU) ---
panns-inference>=0.1.1

# --- Folk-relevance semantic gate (primary) + CLAP fallback (secondary) ---
sentence-transformers>=2.6.0
laion-clap>=1.1.4

# --- Duplicate detection (Chromaprint fingerprinting) ---
pyacoustid>=1.3.0

# --- Post-accept transcription ---
# Remote API backend (default): only needs requests.
requests>=2.31.0

# Local faster-whisper backend (optional, only if transcription.backend = "local"):
# faster-whisper>=1.0.0
# ctranslate2>=4.0.0
# transformers>=4.38.0
